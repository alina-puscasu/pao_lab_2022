package pao.classes;

// special "class" that represents a group of constants
// to create an enum, use the enum keyword (instead of class), and separate the constants with a comma
// can access enum constants with the dot syntax
public enum CatBreed {
    BIRMAN,
    BRITISH_SHORT_HAIR,
    HIMALAYAN
}
